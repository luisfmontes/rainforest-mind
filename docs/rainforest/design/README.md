# Designs aprovados

Um arquivo por trabalho, escrito pela skill `brainstorm` no fim da entrevista:
`AAAA-MM-DD-<tema>.md`. Registra as decisoes fechadas **com o porque de cada
uma**, o que ficou fora de escopo e o que continua aberto.

Versionado de proposito — ver `../README.md`.
