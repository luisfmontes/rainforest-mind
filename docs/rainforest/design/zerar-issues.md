# Zerar as Issues abertas — 19 defeitos, um PR

## Objetivo
Fechar, com correção e teste, as 19 Issues abertas em 2026-09-08 que não são a
#195 (essa foi no fluxo `statusline-reset-e-principal-na-main`, versão 1.9.0).
O pedido do Luís foi literal: "está ficando muito difícil, a cada nova versão
abre 5 issues em vez de corrigir". Triagem feita por dois `planejador` sobre o
código em `58e8c37`, todas as 19 com causa raiz em `arquivo:linha`; o que a
triagem deixou como "decisão do usuário" está decidido abaixo, com o porquê, e
é reversível na revisão do PR.

## Decisões fechadas
- **D1 — #225: `obterOutrosWorktreesComFluxoAberto` deduplica por `(slug, estágio)` antes do `push`** — porquê: a negação "sem estágio ativo" repetia a mesma linha uma vez por worktree (~170 linhas); a mensagem existe para orientar, e 170 linhas iguais não orientam.
- **D2 — #223: `despachar-codex.cjs` recusa (exit 1, `valor invalido: --<flag>`) qualquer um dos quatro valores interpolados no comando (`--worktree`, `--saida`, modelo, esforço) que contenha metacaractere de shell, antes de montar a string; a checagem vive em `hooks/lib/cli-externo.cjs` como `valorSeguroParaShell(v)` para os outros consumidores usarem** — porquê: elimina a injeção pelos quatro pontos hoje, em dois arquivos; a rota estrutural (argv sem shell) exige resolver o shim `.cmd` do Codex no Windows, onde `spawn` sem shell falha com EINVAL, e mexe em cinco chamadores — fica registrada abaixo como caminho avaliado, não como dívida escondida.
- **D3 — #221: `avaliarConfigDir` devolve `{ ...doInstall, dir: configDir }` no ramo sem clone do marketplace, como os outros três retornos** — porquê: `rotular` faz `path.basename(r.dir)` com duas config dirs, e `undefined` ali derruba o `/saude` inteiro na máquina que mais precisa dele.
- **D4 — #220: `gate-publicacao-destino.cjs` ganha o ramo `ehSubagente` que os outros três gates já têm (subagente não vê `.rainforest-gate-off` nem `RAINFOREST_GATE_OFF`), e uma bateria de regressão única, `hooks/testa-fuga-de-escotilha.sh`, roda os quatro gates com `agent_id` e afirma a ausência das duas strings** — porquê: dois dos três arquivos citados na Issue já estavam corrigidos sem teste que prove isso, e o terceiro nunca teve o ramo; a bateria é o que impede a quarta vez.
- **D5 — #218: todo subcomando do `estado.cjs` recusa com exit 1 (`flag desconhecida: --xxx`) qualquer `--flag` fora da lista aceita daquele subcomando, antes de gravar** — porquê: `--dry-run` em `marcar` gravou design aprovado; flag ignorada em silêncio é o pior dos dois erros.
- **D6 — #210: `conferir-entrega.cjs` com commit do agente vazio (diff `base..commit` sem arquivo) reprova (`falha: commit do agente vazio — entrega inexistente`), com ou sem `--paralelo`; `--paralelo` sem `--espera` continua aviso** — porquê: entrega vazia nunca é integrável, então não há caso legítimo a preservar; exigir `--espera` mudaria o contrato dos briefings existentes por um ganho que a checagem 4 já dá quando o conjunto não é vazio.
- **D7 — #207: `poda-estagio.cjs` delega a `estagio-ativo.cjs.resolver()` em vez de reimplementar a extração de slug e branch** — porquê: cópia divergente do resolvedor canônico, verde na bateria porque a bateria só usava branch sem `fluxo/`; é o mesmo defeito que o próprio `estagio-ativo.cjs` documenta ter eliminado em si.
- **D8 — #196: `relatorios/` entra em `globs_isentos` do creep** — porquê: relatório é registro do fluxo, não entrega do plano; por construção não pode ter tarefa.
- **D9 — #192: `conferir-fluxo.cjs` ganha o subcomando `mutacoes --slug <slug>`, que lê os blocos `mutacao:` do plano (`arquivo`, `de`, `para`, `bateria`; `n/a` pula) e roda `conferir-mutacao.cjs` para cada um, exit ≠ 0 se algum mutante sobrevive; `estado.cjs marcar --estagio verificar --status ok` roda esse subcomando e recusa quando ele reprova (plano sem nenhum bloco `mutacao` literal passa, listando os pulados)** — porquê: a regra de mutação era prosa em duas skills e nenhum executável a chamava; `marcar plano ok` e `marcar design aprovado` já rodam validador antes de gravar, então o ponto de encaixe existe e tem precedente. A checagem de forma do retorno do agente (árvore suja, sem commit) o `conferir-entrega.cjs` já faz.
- **D10 — #216: os 11 arquivos `testa-*.sh` citados passam a limpar toda sandbox no `trap EXIT` (idioma `SANDBOXES` + `nova_sandbox()` + `cleanup()` de `skills/montar-corpus/testa-build.sh` nos 8 multi-sandbox; `trap` simples nos 3 sem nenhum), e uma bateria estática nova, `scripts/testa-sandbox-com-trap.sh`, reprova qualquer `testa-*.sh` que tenha `mktemp -d` sem `trap ... EXIT`, ou mais de um `mktemp -d` sem o idioma `SANDBOXES`** — porquê: 1.252 pastas órfãs medidas; a guarda dinâmica (matar cada bateria por timeout e contar `tmp.*`) custaria minutos por rodada de CI, e a estática pega a forma que causa o vazamento.
- **D11 — #215: `conferir-mutacao.cjs` extrai o placar (`ok: N   falhou: M`, ou `N ok, M falhou`) do stdout do baseline e do mutante e recusa (exit 6, `queda desproporcional de asserções`) quando a queda de `ok` passa de 20% do baseline, mesmo com exit ≠ 0; sem placar reconhecível, segue como hoje** — porquê: mutação que derruba a bateria inteira por `ReferenceError` passava por prova; o placar é a diferença entre "o teste pegou" e "o teste quebrou".
- **D12 — #214: o hook de abertura (`hooks/foco-session-start.cjs`) imprime uma linha quando o checkout principal do repositório está N commits atrás de `origin/main` (com o `git -C <principal> pull --ff-only` pronto para copiar) e lista worktrees cuja branch já está contida em `origin/main` (candidatas a `limpar`)** — porquê: sessão isolada em worktree não pode atualizar o principal, e isso é do harness; o que o plugin pode é tirar o silêncio.
- **D13 — #208: `hooks/ferramentas-consulta.cjs` lê o timeout de `RFM_FERRAMENTAS_TIMEOUT_MS` (default 2000) e a bateria ganha o caso do caminho `incerto` ("prossigo sem garantia", exit 0, sem gravar no ledger)** — porquê: o ramo existe e é o comportamento certo da regra 14; sem caso, ambiente lento reprova a bateria por fazer a coisa certa.
- **D14 — #200: `limpar-worktrees.cjs` lê `sessoes.json` (o mesmo que `heartbeat.cjs` escreve) e reclassifica como `de-outra-sessao` o worktree cujo `cwd` bate com sessão viva que não é esta; `--remover` não o remove, só lista** — porquê: classificar por sujeira nunca distingue "limpo porque acabou" de "limpo porque a outra sessão acabou de commitar e ainda está lá".
- **D15 — #199: o rodízio de `foco.cjs` (teto 10, regex de nome, desempate no mesmo milissegundo) vira `scripts/lib/backup-rotativo.cjs` e é chamado por `ideias.cjs`, `divergencias.cjs` e `ferramentas.cjs`** — porquê: 430 cópias e 232 MB medidos; o molde certo já existia num vizinho, e três `copyFileSync` crus são o mesmo defeito três vezes.
- **D16 — #197: `referencias/perfil-de-trabalho.md` ganha as duas linhas ("toda troca de texto por script leva asserção de contagem" e "confira que a peça nova é chamada, não só que existe"), propagadas aos nove `agents/*.md` por `perfil.cjs --aplicar`** — porquê: mecanismo morto entregue com bateria verde; a fonte única do bloco de método é onde a regra passa a valer para todo agente.
- **D17 — #194: `atualizar-cli.sh` ganha o lock por `mkdir` atômico com órfão de 5 min que `statusline-versao.sh` e `statusline-jornada.sh` já têm, e a bateria os quatro casos (simultâneo, órfão, recente, sem rede)** — porquê: dois refreshers com lock e um sem é a corrida esperando a hora.
- **D18 — #193: `conferir-versao.cjs` faz `git fetch origin main` (silencioso, com timeout) antes de comparar com `origin/main`; falha de rede vira aviso e a comparação segue com a ref local; `--sem-fetch` desliga para bateria** — porquê: a comparação semver já existe desde o PR #205, mas contra a ref local — numa sessão de worktree longa ela está velha exatamente quando a segunda sessão bumpa; o conserto é no executável, não na prosa da skill.
- **D19 — Versão 1.10.0** — porquê: D5 e D9 mudam o contrato de `estado.cjs` (flag desconhecida recusa; `verificar ok` roda mutações) para qualquer repositório que use o fluxo — minor, não patch. *(Emenda 2026-09-12: a main já está em 1.11.0 — a fatia limpa saiu como parte da 1.10.0 e a 1.11.0 veio de outro fluxo. Este PR fecha como **1.12.0**: D26 acrescenta gate e D28 muda o contrato do `marcar`, os dois minor.)*
- **D20 — #223, achado da revisão: `valorSeguroParaShell` recusa valor terminado em contrabarra, e `despachar-codex.cjs` normaliza `--worktree` com `path.resolve` antes de validar** — porquê: o chamador envolve o valor em aspas duplas; no Windows uma contrabarra final escapa a aspa de fechamento e os argumentos seguintes são engolidos (reproduzido com eco de argv: `-C`, `-c approval_policy=never` e `-o` viraram um argumento só). Não é o metacaractere clássico, é a interação entre o valor "seguro" e as aspas do chamador. Recusar contrabarra final é regra de uma linha; caminho de diretório sem separador final é o mesmo diretório, então nada legítimo se perde.
- **D21 — #192, achado da revisão: `estado.cjs` passa `--plano <plano.arquivo>` ao `conferir-fluxo.cjs mutacoes` que dispara em `marcar verificar ok`, e a fixture `t6b` afirma que a saída contém `mutante sobreviveu`** — porquê: a T21 consertou o `existsSync` que decide SE a catraca roda e parou ali; o subprocesso resolvia `docs/rainforest/planos/<slug>.md` fixo e ignorava `plano.arquivo`. Fluxo com plano de outro nome nunca fecha `verificar`; com colisão de nome, valida o plano errado em silêncio. E o `t6b` passava pelo motivo errado — o exit 2 vinha de "plano não existe", não do mutante sobrevivente. Afirmar a mensagem do mutante é o que separa os dois motivos.
- **D22 — #216, achado da revisão: a guarda `testa-sandbox-com-trap.sh` fecha os três buracos demonstrados — `SANDBOXES` e `trap` só contam fora de comentário; a linha do `trap ... EXIT` (ou a função que ela nomeia) tem de conter `rm -rf`; `mktemp -d` dentro de corpo de função só passa se a função registra em `SANDBOXES+=`, senão conta como múltiplo** — porquê: casador de substring não é análise de que o trap limpa o que foi criado: trap com corpo vazio passava, `SANDBOXES` em comentário passava, `mktemp -d` numa função chamada duas vezes contava como uma ocorrência. As três formas viram fixtures do `--autoteste`.
- **D23 — #208, achado da revisão: em `hooks/testa-ferramentas-consulta.sh` o `EXIT=$?` captura o exit do `node` do hook, imediatamente após a linha que o roda, nos dois casos de timeout** — porquê: hoje ele captura o exit do `unset` (que sempre sai 0), então a asserção "ok exit 0 (D10)" nunca pode falhar. Medidor que não sabe falhar é o defeito da rodada inteira.
- **D24 — #230: no Windows, `conferir-mutacao.cjs` roda a `--bateria` por `bash -c` quando `bash` está no PATH; sem `bash`, recusa (exit 1) bateria que contenha `;`, `&&` ou `||`, porque o `cmd.exe` não separa comandos e entregaria o resto como argumento; a ajuda deixa de prometer que `SHELL=/bin/bash` tem efeito** — porquê: `spawnSync(..., {shell: true})` usa o `ComSpec` e ignora `SHELL`; em 2026-09-11 `rm -rf __pycache__; python testes/x.py` entregou ao `rm` os tokens seguintes como arquivos a apagar, e o baseline saiu VERDE em 79 ms sobre uma bateria que já não existia. A recusa custa uma linha e evita destruição silenciosa; o `bash` explícito é o que as próprias baterias deste repositório já assumem.
- **D25 — #232: a bateria roda com `PYTHONDONTWRITEBYTECODE=1` no ambiente do `spawnSync`, no baseline e na pós-mutação** — porquê: a catraca restaura o fonte e deixa o `.pyc` mutado; quando a mutação tem o mesmo número de bytes, o par (mtime, tamanho) confere e o Python serve a versão mutada dali em diante — a rodada SEGUINTE mede outro código. Não escrever bytecode não apaga arquivo de ninguém e vale para qualquer bateria Python; apagar `__pycache__` mexeria em arquivo que a catraca não criou.
- **D26 — #235: gate novo `hooks/gate-verificador-staged.cjs` (PreToolUse em `Bash`, dispara em `git commit`) roda o verificador do repositório sobre o conteúdo STAGED e barra o commit (exit 2, com a saída do verificador) quando ele reprova. Descoberta do verificador, nesta ordem: chave `"verificador-staged"` em `.rainforest/config.json` (comando); senão `scripts/check-personal-data.py|.cjs|.sh|.js`; senão, se existir `scripts/conferir-publicacao.cjs`, ele mesmo lendo cada arquivo staged por stdin (`-`). O gate materializa os blobs staged (`git show :<caminho>`) numa pasta temporária preservando os caminhos relativos e chama o verificador com esses caminhos como argumentos e cwd nessa pasta; repo sem verificador passa; mesma família dos outros gates (escotilhas honradas e escondidas do subagente, registrado em `hooks/hooks.json`, entra em `testa-fuga-de-escotilha.sh`)** — porquê: `gate-publicacao-destino` só vê `Write`/`Edit`; valor gravado por script (`python patch.py`) chega ao commit e só a CI pega — do lado irreversível, onde `filter-branch` em rede de fork não basta (Issue #83). Commit local é reversível; push não. É o conteúdo staged que vira histórico, por isso não se olha o working tree. Descartado gravar `.git/hooks/pre-commit`: altera o ambiente do usuário e morre em clone novo.
- **D27 — #237: a regra `telefone` de `conferir-publicacao.cjs` isenta o match imediatamente precedido por prefixo de identificador de plataforma — `runs/`, `jobs/`, `issuecomment-`, `pull/`, `issues/`, `/commit/`, `discussion_r`** — porquê: id de run do Actions (11 dígitos) e de comentário de PR (10 dígitos) casam a forma de telefone e nenhuma das três isenções os alcança; a mensagem manda "confirmar que é ID" e não existe como confirmar sem desligar o gate inteiro. A isenção por contexto tem a mesma forma da isenção de hex (que já olha ±5 caracteres), não cria ritual e não mexe na faixa de dígitos — alargar a faixa reabriria o falso negativo que criou a regra.
- **D28 — #238: `estado.cjs marcar` recusa com exit 2 (a) `--status parcial` num estágio quando um estágio POSTERIOR está `ok`, `aprovado` ou `parcial`, nomeando-o — posterior `reprovado` é a reabertura sancionada e continua passando; (b) `carimbos[].tarefa` fora de `1..tarefas` quando `tarefas` já está gravado, nomeando o número; (c) `marcar` num estágio cujo pré-requisito (`PRE_REQUISITOS`) está `pendente`. E `skills/executar/SKILL.md` diz, ao lado do exemplo do carimbo, que ele se prova num slug de caixa de areia** — porquê: um revisor copiou o exemplo da skill e rodou contra o slug real: virou `executar` de `ok` para `parcial` por baixo de um `revisar` com `em_voo`, gravou carimbo da tarefa 99 num plano de 1 tarefa e `proximo` passou a devolver `executar`. Só o `exigir` conhecia a ordem; o `marcar` gravava o que recebesse.
- **D29 — #231: (1) `skills/revisar/SKILL.md` ganha o molde do briefing do revisor com a cláusula fixa "não mute; descreva a mutação (linha, teste que quebraria), quem a executa é o `tester` isolado", e `agents/revisor.md` manda recusar, na primeira linha do relato, briefing que peça mutação; (2) `gate-worktree.cjs` continua barrando o restauro do subagente no checkout principal, mas a mensagem de bloqueio nomeia o arquivo e o comando de restauro para a janela principal (`git -C <toplevel> checkout -- <arquivo>`) e avisa que a árvore pode ter ficado suja pelo agente** — porquê: as duas instruções (não edite; prove por mutação) conviviam no briefing e o agente pagava. O skill e o agente já proíbem mutar; o que faltava era o molde do briefing carregar a cláusula, para a janela que despacha não a contradizer. Deixar o restauro passar foi descartado: subagente não sabe de quem é o diff, e `checkout --` para o HEAD apaga edição não commitada do usuário no mesmo arquivo; o que se pode é tirar o silêncio da sujeira.

## Emenda de 2026-09-12 — rodada 2

O `revisar` reprovou a primeira execução com 5 achados (todos "bateria verde pelo motivo errado" ou cobertura que não media), e a fatia sem achado saiu pela PR #236 com 12 Issues. Ficaram #192 #196 #208 #216 #218 #223; D20–D23 são os achados virando decisão. No mesmo dia abriram #230 #231 #232 #235 #237 #238 — D24–D29. #206 continua descartada como abaixo e fecha com a evidência da concordância atual. As decisões seguem a recomendação registrada em cada Issue, com o porquê escrito; são reversíveis na revisão do PR.

## Avaliado e descartado
- **#230, suspeitar de baseline verde em tempo absurdo** — o conserto na raiz (`bash -c`) elimina o cenário que produziu os 79 ms; um teto de "rápido demais" não tem número que não seja arbitrário, e a T22 deste mesmo plano já mostrou que proporção fixa pune bateria pequena.
- **#237, marcador de confirmação na linha (opção b)** — cria ritual em trava que dispara toda semana, e ritual é como se aprende a ignorar a saída; a isenção por prefixo resolve o caso medido sem campo para alguém preencher.
- **#231, gate deixar passar `git checkout -- <arquivo>` do subagente no checkout principal** — ver D29.
- **#235, hook `.git/hooks/pre-commit` gravado pelo plugin** — ver D26.
- **#223, rota argv sem shell** (mudar `rodarCli` para `spawnSync(cmd, args)` nos cinco chamadores) — no Windows o `codex` é shim `.cmd`, e Node ≥ 18.20 recusa `spawn` de `.cmd` sem shell (EINVAL); resolver o binário real ou passar por `cmd.exe /c` com argv devolve o problema de quoting do `cmd.exe`. Custo G, ganho igual ao de D2 para os quatro valores que existem hoje.
- **#206, mecanismo geral de coerência design × SKILL.md** — a instância concreta (`--repo`/`--corpus`) já está corrigida (nota de 2026-09-05 no design D9 do corpus; código e skill concordam). O mecanismo exigiria marcação nova ligando seção de skill a decisão de design; marcação que ninguém preenche é o modo de falha da #197. A Issue fecha com a evidência da concordância atual, e o gancho fica aqui.
- **#216, guarda dinâmica** (rodar cada bateria com `timeout` e contar `tmp.*` antes e depois) — minutos por rodada de CI para provar o que a checagem estática de forma prova em milissegundos.
- **#210, exigir `--espera` com `--paralelo`** — muda o contrato dos briefings já escritos; a checagem 4 só é cega com conjunto vazio, e D6 fecha esse caso na raiz.
- **#192, direção (a), agente confirmar por `SendMessage`** — a própria Issue a descartou: prosa mais forte foi tentada duas vezes.

## Fora de escopo
- #195 (checkout principal na `main`) — fluxo `statusline-reset-e-principal-na-main`, versão 1.9.0.
- Reescrever `rodarCli` para argv (ver acima).
- Migrar planos antigos para `mutacao.de` literal — D9 só roda o que o plano declara; bloco em prosa é reportado como pulado (`de` não casa), não como reprovação.

## Em aberto
- Nada. As quatro "decisões do usuário" da triagem (#223, #206, #210, #192) estão fechadas acima com a recomendação do planejador seguida em três e o porquê registrado na quarta; a aprovação deriva do pedido do Luís de 2026-09-08 ("zerar as issues") e da autorização de subagentes nesta sessão.
