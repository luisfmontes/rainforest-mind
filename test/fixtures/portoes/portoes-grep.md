# Portões: oráculo que depende de ferramenta Unix

- [ ] P1: o log de deploy não contém linha de rollback
  CHECK: grep -c rollback deploy.log
  ESPERA: contagem consolidada do deploy
  EVIDENCIA: pendente
